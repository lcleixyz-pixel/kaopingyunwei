# 考评分支机构管理系统 — 深化设计方案

> 文档版本：V2.0  
> 更新日期：2026年5月4日  
> 本次更新：AI运维架构 · 数据迁移方案 · AI开发规范 · 引导式需求澄清

---

## 本轮核心新增诉求

| 编号 | 诉求 | 影响范围 | 应对策略 |
|------|------|---------|---------|
| R1 | **总部集中部署，但便于迁移** | 部署架构、数据层、备份策略 | 容器化+SQLite单文件+导出/导入工具链 |
| R2 | **唯一非专业运维 + AI运维** | 运维界面、系统架构、功能设计 | 内置AI运维助手，自然语言操作 |
| R3 | **AI开发规范/代码知识库** | 代码规范、文档标准、拓展机制 | AI-First代码规范、知识库即代码 |
| R4 | **多轮引导式对话明确需求** | 需求工程、边界确认 | 本轮提出5个关键引导问题 |

---

## 第一部分：AI运维架构设计

### 1.1 设计理念："用说话代替操作"

您是唯一的非专业运维，没有7×24的技术团队。系统的运维界面应该像**和一个技术专家对话**一样简单——用自然语言描述问题或意图，AI完成分析和操作。

```
传统运维方式                    AI运维方式
─────────────────────────────────────────────────
登录服务器                      打开系统后台
↓                              ↓
查日志文件                      说："最近系统有什么异常吗？"
↓                              ↓
grep找错误关键词                 AI自动分析日志+给出结论+建议
↓                              ↓
手动执行SQL修复                  说："帮我修复这个问题"一键确认
```

### 1.2 AI运维助手功能矩阵

#### 1.2.1 系统健康监控（问一问就知道）

| 功能 | 用户说 | AI做 |
|------|--------|------|
| 状态总览 | "系统现在运行正常吗？" | 检查CPU、内存、磁盘、数据库连接、API响应时间，生成健康报告 |
| 性能分析 | "最近系统变慢了吗？" | 分析近7天响应时间趋势，识别慢查询，给出优化建议 |
| 容量预警 | "磁盘空间还够多久？" | 分析增长趋势，预测磁盘/存储用尽时间，提前预警 |
| 用户统计 | "今天有多少人登录？" | 实时统计活跃用户、登录次数、操作分布 |

#### 1.2.2 数据管理（备份恢复一句话）

| 功能 | 用户说 | AI做 |
|------|--------|------|
| 一键备份 | "帮我备份一下数据" | 立即执行全量备份，生成备份文件，报告备份大小和位置 |
| 备份验证 | "备份文件完好吗？" | 校验备份文件完整性，报告可恢复性 |
| 数据恢复 | "恢复到昨天凌晨的备份" | 列出可选备份点，确认后执行恢复，报告恢复结果 |
| 数据导出 | "导出第一季度的所有考评数据" | 按条件筛选，生成加密导出包，供下载或迁移 |
| 数据迁移 | "我要把系统搬到新电脑上" | 一键打包全部数据+配置+Docker镜像，新机器上一键恢复 |

#### 1.2.3 用户与权限管理（不用懂技术）

| 功能 | 用户说 | AI做 |
|------|--------|------|
| 重置密码 | "给张三重置密码" | 确认身份后重置，生成临时密码并通知 |
| 权限查询 | "李四能做什么操作？" | 列出该用户的所有权限，解释每项权限的含义 |
| 添加用户 | "给XX分支机构新增一个管理员" | 引导填写必要信息，自动分配合理权限 |
| 操作审计 | "查一下上周谁修改了成绩" | 检索审计日志，用自然语言报告操作时间、人员、内容 |

#### 1.2.4 故障诊断（AI自动排障）

| 功能 | 用户说 | AI做 |
|------|--------|------|
| 自动告警 | （无需人工） | 系统异常自动发送告警消息到微信/邮件，附带AI分析结论 |
| 日志分析 | "成绩录入报错了，怎么回事？" | 自动定位相关错误日志，分析原因，给出修复建议 |
| 修复建议 | "这个问题怎么解决？" | 基于知识库给出分步解决方案，复杂问题提供一键修复 |
| 升级助手 | "帮我更新到最新版本" | 检查当前版本，下载更新，自动备份，执行升级，验证结果 |

### 1.3 AI运维技术架构

```
┌──────────────────────────────────────────────────────┐
│                  运维交互层                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────────┐ │
│  │ 自然语言输入 │  │  一键按钮   │  │ 自动触发(告警)  │ │
│  │ "帮我备份"  │  │ "立即备份"  │  │ 异常自动通知    │ │
│  └─────┬──────┘  └─────┬──────┘  └───────┬────────┘ │
└────────┼───────────────┼───────────────┼──────────┘
         │               │               │
         └───────────────┼───────────────┘
                         ▼
┌──────────────────────────────────────────────────────┐
│              AI运维引擎 (本地运行)                       │
│  ┌────────────────────────────────────────────────┐ │
│  │  意图理解：用户想做什么？                          │ │
│  │  参数提取：从对话中提取关键信息（时间/人员/范围）    │ │
│  │  操作映射：映射到具体的系统操作                     │ │
│  │  安全确认：敏感操作需要二次确认                     │ │
│  │  结果生成：用自然语言报告操作结果                   │ │
│  └────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────┐ │
│  │  运维知识库 (内置常见问题与解决方案)                │ │
│  │  • 500+ 常见运维场景问答                          │ │
│  │  • 故障诊断决策树                                 │ │
│  │  • 操作手册（自然语言版）                          │ │
│  │  • 版本更新日志与变更说明                          │ │
│  └────────────────────────────────────────────────┘ │
└──────────────────────┬───────────────────────────────┘
                       │
         ┌─────────────┼─────────────┐
         ▼             ▼             ▼
┌────────────┐  ┌────────────┐  ┌────────────┐
│  系统监控    │  │  数据库操作  │  │  文件管理    │
│ (CPU/内存/  │  │ (SQLite操作 │  │ (备份/导出/  │
│  磁盘/网络)  │  │ /查询/维护) │  │ 迁移/日志)   │
└────────────┘  └────────────┘  └────────────┘
```

### 1.4 AI运维界面原型（概念）

```
┌──────────────────────────────────────────────────────────┐
│  🏠 首页    📊 仪表盘    ⚙️ 系统设置    🤖 AI运维助手      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  🤖 AI运维助手 — 您的智能技术顾问                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │                                                    │  │
│  │  💬 试试这样问我：                                  │  │
│  │  • "系统运行状态如何？"                              │  │
│  │  • "备份今天的数据"                                  │  │
│  │  • "最近有什么异常？"                                │  │
│  │  • "导出3月份的考评记录"                             │  │
│  │                                                    │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ > [在此输入您的问题或指令...              ] [发送]  │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ─── 快捷操作（一键执行，无需输入） ─────────────────────  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 🔄 一键   │ │ 💾 一键   │ │ 📦 导出   │ │ 🔍 查看   │   │
│  │   备份   │ │   恢复   │ │   数据   │ │   日志   │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│                                                          │
│  ─── 最近动态 ─────────────────────────────────────────  │
│  ✅ 05-04 02:00 自动备份完成 (大小: 15.2MB)               │
│  ✅ 05-03 14:23 系统版本 v1.2.0 升级成功                  │
│  ⚠️ 05-03 09:15 磁盘使用率超过80% (已自动清理临时文件)     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 第二部分：数据迁移与可移植性设计

### 2.1 迁移场景分析

| 场景 | 描述 | 发生频率 | 解决方案 |
|------|------|---------|---------|
| S1 整机迁移 | 换服务器/换云厂商 | 1-3年一次 | 一键导出/导入工具 |
| S2 本地→云端 | 从办公室电脑迁到云服务器 | 可能一次 | Docker镜像+数据包 |
| S3 云端→本地 | 从云端迁回办公室 | 可能一次 | 同上 |
| S4 灾难恢复 | 硬盘损坏后恢复 | 不确定 | 定期自动备份+一键恢复 |
| S5 多副本部署 | 给新分支机构快速部署 | 随业务增长 | 标准化镜像+配置模板 |

### 2.2 "一键迁移"技术方案

#### 2.2.1 数据打包（导出）

```
您说："我要迁移系统"
系统做：
  1. 导出 SQLite 数据库文件 → exam_data_20260504.db
  2. 导出 上传的文件（证书/档案/头像） → attachments.zip
  3. 导出 系统配置 → config.json
  4. 导出 操作日志 → audit_logs.json
  5. 打包为一个加密的迁移文件 → exam_system_backup_20260504.pkg
  6. 生成校验码，确保文件完整
  7. 提供下载链接
```

#### 2.2.2 数据恢复（导入）

```
新机器上：
  1. 安装 Docker（一次性）
  2. 下载系统镜像（docker pull）
  3. 上传迁移文件 exam_system_backup_20260504.pkg
  4. 说："恢复系统"
  5. 系统自动：校验 → 解密 → 恢复数据库 → 恢复文件 → 恢复配置 → 完成
  6. 打开浏览器，一切如初
```

### 2.3 容器化设计（环境一致性保障）

```yaml
# docker-compose.yml - 系统的完整定义
version: '3.8'

services:
  app:
    image: exam-system:v1.2.0
    ports:
      - "3000:3000"
    volumes:
      # 数据持久化 - 这三个目录就是全部数据
      - ./data:/app/data        # SQLite数据库
      - ./files:/app/files      # 上传的文件
      - ./config:/app/config    # 系统配置
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # AI运维助手服务（独立容器，可选）
  ai-ops:
    image: exam-system-ai-ops:v1.0
    volumes:
      - ./data:/app/data:ro     # 只读访问数据库
      - ./logs:/app/logs        # 日志分析
    environment:
      - AI_OPS_ENABLED=true
    restart: unless-stopped

  # 自动备份服务
  backup:
    image: exam-system-backup:v1.0
    volumes:
      - ./data:/app/data:ro
      - ./backups:/app/backups  # 备份存储
      - ./config:/app/config:ro
    environment:
      - BACKUP_SCHEDULE=0 2 * * *  # 每天凌晨2点
      - BACKUP_RETENTION_DAYS=30
    restart: unless-stopped
```

> **关键设计**：所有数据都在 `./data`、`./files`、`./config` 三个目录中。迁移时只需打包这三个目录，任何地方都能原样恢复。

### 2.4 数据迁移能力对比

| 能力 | 传统系统 | 本系统设计 |
|------|---------|-----------|
| 备份操作 | 需懂命令行 | 一键备份/说句话就行 |
| 恢复操作 | 需懂SQL+命令行 | 上传文件+一键恢复 |
| 跨平台迁移 | 复杂（环境依赖） | Docker保证一致，任意平台 |
| 迁移验证 | 手动检查 | 自动校验完整性 |
| 迁移学习成本 | 高（需培训） | 零（AI引导） |

---

## 第三部分：AI开发规范与代码知识库

### 3.1 核心原则："代码即知识库"

为了让AI能准确理解代码、辅助后续拓展，代码本身就是最好的知识库。我们采用**AI-First**的开发规范——写代码时就考虑AI如何理解它。

### 3.2 目录结构规范（AI可理解）

```
exam-system/
├── 📁 docs/                          # 📚 知识库核心
│   ├── 📄 ARCHITECTURE.md            #   系统架构说明
│   ├── 📄 API.md                     #   API接口文档
│   ├── 📄 DATABASE.md                #   数据库结构说明
│   ├── 📄 DEPLOYMENT.md              #   部署指南
│   ├── 📄 AI_CONTEXT.md              #   AI开发上下文（关键！）
│   └── 📁 decisions/                 #   技术决策记录
│       ├── 📄 001-why-sqlite.md
│       ├── 📄 002-why-react.md
│       └── 📄 003-auth-design.md
│
├── 📁 src/
│   ├── 📁 frontend/                  # 🎨 前端代码
│   │   ├── 📁 components/            #   UI组件
│   │   │   ├── 📁 ExamNode/         #   考评节点组件
│   │   │   ├── 📁 Reminder/         #   提醒组件
│   │   │   └── 📁 Dashboard/        #   仪表盘组件
│   │   ├── 📁 pages/                 #   页面
│   │   ├── 📁 hooks/                 #   业务逻辑钩子
│   │   ├── 📁 stores/                #   状态管理
│   │   └── 📁 utils/                 #   工具函数
│   │
│   ├── 📁 backend/                   # ⚙️ 后端代码
│   │   ├── 📁 routes/                #   API路由
│   │   │   ├── 📄 exam-nodes.ts     #   考评节点API
│   │   │   ├── 📄 reminders.ts      #   提醒API
│   │   │   ├── 📄 auth.ts           #   认证API
│   │   │   └── 📄 export.ts         #   导出API
│   │   ├── 📁 services/              #   业务服务
│   │   ├── 📁 models/                #   数据模型
│   │   ├── 📁 middleware/            #   中间件
│   │   ├── 📁 jobs/                  #   定时任务
│   │   └── 📁 ai-ops/                #   AI运维模块
│   │
│   └── 📁 shared/                    # 🔗 前后端共享
│       └── 📁 types/                 #   TypeScript类型定义
│
├── 📁 scripts/                       # 🔧 运维脚本
│   ├── 📄 backup.sh                  #   一键备份
│   ├── 📄 restore.sh                 #   一键恢复
│   ├── 📄 migrate.sh                 #   数据迁移
│   └── 📄 upgrade.sh                 #   版本升级
│
├── 📁 docker/                        # 🐳 Docker配置
│   ├── 📄 Dockerfile
│   └── 📄 docker-compose.yml
│
└── 📄 README.md                      # 📖 项目说明（AI友好的索引）
```

### 3.3 AI_CONTEXT.md — AI开发核心知识库

这是一个特殊的文件，专门为AI（比如ChatGPT、Claude等）提供项目上下文，让AI能快速理解系统并生成符合规范的代码。

```markdown
# AI开发上下文 — 考评分支机构管理系统

## 项目概况
- 名称：考评分支机构管理系统 (exam-system)
- 用途：职业技能等级认定考务全流程管理
- 技术栈：React + TypeScript + Node.js + SQLite + Docker
- 架构：多租户SaaS，总部集中部署

## 数据库核心表
| 表名 | 用途 | 关键字段 |
|------|------|---------|
| tenants | 分支机构（租户） | id, name, code, status |
| users | 用户 | id, tenant_id, role, username |
| exam_plans | 考评计划 | id, tenant_id, exam_date, status |
| exam_nodes | 考评节点 | id, plan_id, node_type, deadline, status |
| candidates | 考生 | id, tenant_id, name, id_card, level |
| scores | 成绩 | id, candidate_id, score, verifier |
| certificates | 证书 | id, candidate_id, cert_no, issue_date |
| archives | 档案 | id, plan_id, file_path, seal_hash |
| audit_logs | 审计日志 | id, user_id, action, target, timestamp |

## 开发规范
1. 所有API返回统一格式：{ success: boolean, data?: T, error?: string }
2. 所有数据库操作必须通过服务层，禁止直接在路由中写SQL
3. 所有敏感操作记录审计日志
4. 前端组件使用函数式组件 + Hooks
5. 类型定义放在 shared/types/ 目录

## 拓展指南
- 新增考评节点：修改 exam_nodes 表 + 添加节点类型枚举 + 前端组件
- 新增报表：在 backend/services/report.ts 中添加查询方法
- 新增提醒规则：在 backend/jobs/reminder.ts 中添加定时任务
```

### 3.4 API接口规范（AI可生成）

```typescript
// 统一响应格式
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
  timestamp: string;
  requestId: string;  // 用于追踪和调试
}

// 考评节点API示例
// GET /api/exam-nodes?planId=xxx&status=pending
// 返回：{ success: true, data: ExamNode[], timestamp: "2026-05-04T10:00:00Z" }

// POST /api/exam-nodes/{id}/complete
// 请求体：{ completedAt: string, notes?: string, attachments?: string[] }
// 返回：{ success: true, data: { nodeId, status: "completed" }, timestamp: "..." }

// 错误示例
// { success: false, error: "节点已逾期，请联系管理员", timestamp: "..." }
```

### 3.5 代码注释规范（AI可读）

```typescript
/**
 * 完成考评节点
 * 
 * @businessRule 节点完成后自动触发下一个节点的提醒
 * @businessRule 如果节点逾期，需要记录逾期原因
 * @audit 记录操作人、时间、IP地址
 * @param nodeId 节点ID
 * @param data 完成数据
 * @returns 更新后的节点状态
 * 
 * @example
 * completeNode("node_123", {
 *   completedAt: "2026-05-04T10:00:00Z",
 *   notes: "已按时完成考场编排"
 * })
 */
async function completeNode(
  nodeId: string,
  data: CompleteNodeRequest
): Promise<ApiResponse<ExamNode>> {
  // 1. 校验节点存在且属于当前租户
  // 2. 校验操作权限
  // 3. 更新节点状态
  // 4. 记录审计日志
  // 5. 触发下游提醒
  // ...
}
```

### 3.6 拓展机制：AI友好的插件系统

为了让AI能帮您添加新功能，系统设计一个**声明式插件机制**：

```typescript
// 新增一个考评节点类型，只需这样声明：
const NewNodeType = {
  id: "certificate_reissue",           // 节点标识
  name: "证书补办",                     // 显示名称
  description: "处理考生证书补办申请",   // 功能说明
  deadlineRule: "申请后5个工作日",       // 时限规则
  requiredFields: ["reason", "attachments"], // 必填字段
  nextNodes: ["archive_update"],       // 后续节点
  reminders: [
    { before: "2d", message: "证书补办即将到期" },
    { before: "1d", message: "证书补办明天到期" }
  ]
};

// AI根据这个声明自动生成：
// ✓ 数据库迁移脚本
// ✓ API接口代码
// ✓ 前端表单组件
// ✓ 提醒规则配置
// ✓ 测试用例
```

### 3.7 代码知识库文档清单

| 文档 | 用途 | 维护方式 |
|------|------|---------|
| README.md | 项目入口，索引所有文档 | 手动维护 |
| ARCHITECTURE.md | 系统架构说明 | 随架构变更更新 |
| API.md | 所有接口定义（OpenAPI格式） | 从代码自动生成 |
| DATABASE.md | 数据库结构+关系图 | 从代码自动生成 |
| AI_CONTEXT.md | AI开发上下文（核心） | 手动维护+自动同步 |
| CHANGELOG.md | 版本变更记录 | 随发布更新 |
| DEPLOYMENT.md | 部署操作指南 | 手动维护 |

---

## 第四部分：引导式对话 — 5个关键澄清问题

经过以上设计，系统的大框架已经清晰。但为了让最终交付物真正符合您的实际场景，我需要通过**引导式对话**进一步明确以下关键问题：

---

### 🔍 问题1：您的"唯一非专业运维"具体是什么情况？

> 这个问题决定AI运维助手的深度和边界。

**请帮我确认：**
- **A.** 您完全不懂编程和技术，连命令行都没用过 → AI运维需要做成"纯图形界面+自然语言"，零技术暴露
- **B.** 您会一些基础操作，比如会复制文件、会安装软件 → 可以在AI助手基础上暴露一些简单操作
- **C.** 您能用AI工具（比如ChatGPT），会向AI提问寻求帮助 → AI运维助手可以做成对话式，和AI工具协同
- **D.** 您有其他技术人员可以临时请教，但不是专职 → 系统设计需要考虑"远程协助"能力

**我的建议**：如果是A或C，建议采用**对话式AI运维助手**——直接在系统后台内置一个"运维助手"界面，像聊微信一样和它对话，它能理解您的需求并执行操作。

---

### 🔍 问题2："便于迁移"的具体场景是什么？

> 这个问题决定迁移工具的粒度和优先级。

**请帮我确认您可能遇到的情况：**
- **场景A：** 可能从现在的办公电脑搬到云服务器（阿里云/腾讯云）→ 需要云部署指南
- **场景B：** 可能从一台电脑换到另一台电脑 → 需要跨机器迁移
- **场景C：** 可能需要给分支机构部署一套独立的 → 需要快速复制部署
- **场景D：** 以上全部 → 需要通用的一键迁移方案

**此外**：您预期的数据量大概是？
- 每年考评人数：____人
- 分支机构数量：____个
- 历史数据保留年限：____年

这些数据影响存储规划和备份策略。

---

### 🔍 问题3："后续拓展"的方向是什么？

> 这个问题决定代码知识库的重点和预留扩展点。

**请帮我确认您未来可能的需求：**
- **功能拓展**：可能新增考评流程节点、新增报表类型、新增提醒方式？
- **规模拓展**：可能从现在的N个分支机构扩展到更多？
- **集成拓展**：可能需要对接人社部门的系统、短信平台、企业微信/钉钉？
- **智能化拓展**：可能想用AI辅助考务（比如AI辅助阅卷、AI生成公示文案）？

**我的建议**：如果主要是"功能拓展"，我们重点做好**声明式配置+AI代码生成**；如果包含"集成拓展"，需要预留标准的Webhook和API对接机制。

---

### 🔍 问题4：分支机构的网络环境如何？

> 这个问题决定"总部集中部署"的具体架构。

**请帮我确认：**
- 分支机构是否都有稳定的互联网连接访问总部系统？
  - **A.** 都有稳定网络 → 纯Web访问，最简单
  - **B.** 部分有，部分没有 → 需要离线模式+数据同步
  - **C.** 大部分不稳定 → 需要考虑独立部署+定期同步

- 分支机构是否有专职人员操作电脑？
  - **A.** 有专职人员 → 可以使用完整Web界面
  - **B.** 兼职人员，操作频率低 → 需要更简化的操作界面
  - **C.** 基本不用电脑 → 需要考虑手机端为主

---

### 🔍 问题5：您对AI辅助开发的具体期望？

> 这个问题决定AI代码知识库的深度和形式。

**请帮我确认：**
- **A.** 希望系统内置AI助手，可以直接在系统里提问"怎么添加一个新功能"，AI给出步骤和代码 → 系统内置AI Copilot
- **B.** 希望有一套完整的开发文档，自己用ChatGPT/Claude等工具时贴给AI作为上下文 → 完善AI_CONTEXT.md
- **C.** 两者都要 → 完整方案
- **D.** 不确定，先让我能用起来，后续再说 → MVP方案，预留扩展

**此外**：您是否已经有使用AI工具（ChatGPT/Claude/文心一言等）的经验？
- 有 → 可以设计更深度的AI协作流程
- 没有 → AI运维助手需要更完善，减少对外部AI的依赖

---

## 第五部分：本轮交付总结与下一步

### 已明确的设计

| 设计项 | 状态 | 说明 |
|--------|------|------|
| 总部集中部署+容器化 | ✅ 确定 | Docker Compose方案，一键部署 |
| SQLite单文件数据库 | ✅ 确定 | 零运维，复制即迁移 |
| AI运维助手 | ✅ 架构确定 | 自然语言交互，覆盖监控/备份/恢复/排障 |
| 一键迁移工具链 | ✅ 方案确定 | 导出/导入/校验全流程 |
| AI开发规范 | ✅ 规范确定 | AI_CONTEXT.md + 声明式插件 + 文档标准 |
| 代码知识库 | ✅ 结构确定 | 标准化目录 + 自文档化代码 |

### 待您确认后推进

1. **回答以上5个引导问题** → 我细化AI运维的具体功能和界面
2. **确认数据规模和分支数量** → 我调整存储和备份策略
3. **明确AI辅助的边界** → 我决定内置AI深度 vs 外部文档
4. **确认拓展优先级** → 我预留对应的扩展点

### 下一步行动（您确认后）

```
您的回答
   ↓
我输出：第三轮需求确认 + UI原型设计 + 数据库设计
   ↓
开始实际开发 Phase 1 MVP
```

---

> 您的系统正在从一个"工具"进化为一个**"智能体"**——它不仅能帮您管理考评业务，还能自己管理自己，还能和AI协同成长。这正是面向未来的设计。
>
> 请回答以上5个问题，我们进入下一轮细化！
